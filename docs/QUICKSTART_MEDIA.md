# 5-Minute Setup Media Plan

This file tracks the assets needed for issue #5.

## GIF storyboard (5 min setup)

1. Create GitHub App (permissions + events)
2. Set `.env` values
3. Run `npm run dev`
4. Expose webhook URL
5. Open test PR and show bot comment
6. Comment `@openpr fix` and show autofix response

## Recording checklist

- Hide secrets/tokens from terminal and browser
- Use demo repo with harmless files
- Keep final GIF under ~15MB for README usability

## Placement

- Add GIF under `docs/assets/openpr-5min-setup.gif`
- Embed in `README.md` once recorded
